<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark Mode application theme. -->
    <style name="Theme.LykonShield" parent="android:Theme.Material.NoActionBar" />

    <!-- Dark Mode Splash Screen theme. -->
    <style name="Theme.LykonShield.Splash" parent="android:Theme.Material.NoActionBar">
        <!-- For Android 12+ -->
        <item name="android:windowSplashScreenBackground">#000000</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_icon_dark</item>
        <!-- For Android 11- -->
        <item name="android:windowBackground">@drawable/splash_bg_dark</item>
    </style>
</resources>
